<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout untuk RemoteViews widget. Tidak semua tag/atribut View biasa
  didukung RemoteViews (contoh: ConstraintLayout TIDAK didukung),
  jadi sengaja pakai LinearLayout/FrameLayout + View standar saja:
  TextView, ImageView, FrameLayout, LinearLayout.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="6dp">

            <ImageView
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:src="@drawable/ic_widget_note"
                android:contentDescription="@string/app_name" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="6dp"
                android:text="@string/app_name"
                android:textSize="11sp"
                android:textStyle="bold"
                android:textColor="#006A6A"
                android:letterSpacing="0.05"
                android:singleLine="true" />
        </LinearLayout>

        <TextView
            android:id="@+id/widgetTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/widget_untitled"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#002020"
            android:maxLines="2"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/widgetBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="@string/widget_empty_body"
            android:textSize="12sp"
            android:textColor="#3F4949"
            android:maxLines="4"
            android:ellipsize="end"
            android:lineSpacingMultiplier="1.3" />

    </LinearLayout>

</FrameLayout>
